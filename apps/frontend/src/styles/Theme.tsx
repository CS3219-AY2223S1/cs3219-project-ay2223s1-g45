import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#FFFFFF',
      light: '#FFFFFF',
      dark: '#000000'
    },
    secondary: {
      main: '#EF429A',
      light: '#EF429A',
      dark: '#AC44B0'
    }
  },
  typography: {
    fontFamily: 'Arimo, Arial'
  }
});
